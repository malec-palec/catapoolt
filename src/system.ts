export const isDev = import.meta.env.DEV;
export const isProd = import.meta.env.PROD;

export const { PI, abs, atan2, cos, floor, max, min, pow, random, sin, round, tan, hypot } = Math;
export const TWO_PI = PI * 2;
